import React from 'react';
import styled from 'styled-components/native';

const ListFooterComponentView = styled.View`
  padding: 5;
  padding-bottom: 55;
`;

const ListFooterComponent = () => (
  <ListFooterComponentView />
);

export default ListFooterComponent;
